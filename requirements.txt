# ============================================
# PC Repair CRM Pro - Dependencies
# ============================================
# Установка: pip install -r requirements.txt
# Для разработки: pip install -r requirements.txt -r requirements-dev.txt
# ============================================

# === 🎨 GUI Framework ===
customtkinter~=5.2.0      # Современный UI на базе Tkinter
# pillow>=11.3.0  # ← Закомментировано временно

# === 🗄️ Database ===
# SQLite встроен в Python 3.7+, дополнительные зависимости не требуются

# === 🔧 Core Utilities ===
python-dotenv>=1.0.0      # Загрузка переменных окружения из .env
chardet>=5.2.0            # Автоопределение кодировки для CSV-импорта
qrcode>=7.4.2             # Генерация QR-кодов для оборудования/запчастей

# === 📤 Export & Reports ===
openpyxl>=3.1.0           # Экспорт отчётов в Excel (.xlsx)
reportlab>=4.0.0        # Генерация PDF (раскомментируйте если нужно)

# === 📧 Optional: Email/SMS Integrations ===
# ⚠️  Раскомментируйте строки ниже только если используете эти функции:
requests>=2.31.0        # HTTP-запросы для SMS API и внешних сервисов
aiosmtplib>=2.0.0       # Асинхронная отправка email (требует asyncio)
twilio>=8.0.0           # SMS-рассылки через Twilio API

# === 🧪 Development & Testing (устанавливать отдельно) ===
# См. requirements-dev.txt для полного списка инструментов разработки

# === 🔍 Type Checking & Linting ===
# Эти пакеты нужны только для разработки, не для продакшена:
# typing-extensions~=4.8.0  # Backport новых возможностей typing
mypy>=1.5.0               # Статическая проверка типов
flake8>=6.1.0             # Linter для PEP8
black>=23.0.0  # ✅ Минимальная версия, без верхнего ограниченияc
pytest>=7.4.0             # Тестирование
pytest-cov>=4.1.0         # Покрытие кода тестами

# === 🪵 Logging Enhancement (опционально) ===
colorlog>=6.7.0           # Цветной вывод логов в консоль

# === 📦 Сборка в .exe ===
# Устанавливается отдельно чтобы не тянуть в продакшен:
pyinstaller>=5.13.0       # Сборка автономного .exe файла
python-dotenv>=1.0.0